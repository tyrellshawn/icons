import { Button } from '@/components/ui/button';

const something = 'dsfsdf';

const TextIcon = () => {
  return (
    <div>
      <Button>{something}</Button>
    </div>
  );
};

TextIcon.displayName = 'TextIcon';

export { TextIcon };
